<!--<%= @admins.inspect %> Printando o valor completo da variável -->
<div class="row">
    <div class="col-lg-12">
        <h1 class="page-header">Lista Admins</h1>
    </div>
    <!-- /.col-lg-12 -->
</div>
<br>
<div class="table-responsive">
  <table class="table table-hover">
    <thead>
      <tr>
        <th>#</th>
        <th>Email</th>
        <th>Ações</th>
      </tr>
    </thead>
    <tbody>
      <% @admins.each do |admin| %>
      <tr>
        <td><%= admin.id %></td>
        <td><%= admin.email %></td>
        <td>
          <%= link_to edit_admins_backoffice_admin_path(admin), class:"btn btn-primary btn-circle" do %>
            <i class="fas fa-edit"></i>
          <% end %>
          <a class="btn btn-danger btn-circle">
            <i class="fas fa-trash"></i>
          </a>
        </td>
      </tr>
      <% end %>
    </tbody>
  </table>
</div>